namespace Tirki.Services;

// Copia questo file in "GoogleAuthService.Secrets.cs" (nome ignorato da git, vedi .gitignore)
// e incolla il vero client secret preso da Google Cloud Console > Credenziali > il tuo
// client OAuth 2.0 "App per Desktop". Senza questo file il progetto non compila: è voluto,
// serve a impedire che il secret finisca per sbaglio nel codice tracciato da git.
public partial class GoogleAuthService
{
    private const string ClientSecret = "INSERISCI_QUI_IL_CLIENT_SECRET";
}
